function N = Divergence_Matrix(point_0,point_t)
%UNTITLED7 Summary of this function goes here
%   Detailed explanation goes here
num = max(size(point_0.points));
diff_point0 = point_
N = 0;
for i = 1:num
    N = N + norm(points(i,:));
end
N = N/num;
end

